{
	"name": "transformation_cleaning",
	"properties": {
		"type": "MappingDataFlow",
		"typeProperties": {
			"sources": [
				{
					"dataset": {
						"referenceName": "DelimitedText4",
						"type": "DatasetReference"
					},
					"name": "source1"
				}
			],
			"sinks": [],
			"transformations": [
				{
					"name": "extractyear"
				}
			],
			"scriptLines": [
				"source(output(",
				"          movieId as string,",
				"          title as string,",
				"          genres as string",
				"     ),",
				"     allowSchemaDrift: true,",
				"     validateSchema: false,",
				"     ignoreNoFilesFound: false) ~> source1",
				"source1 derive(Year = toInteger(trim(right(title, 6), '()'))\r",
				",",
				"          title = ERROR_FUNCTION('toInteger(left(title, length(title)-6), __SINGLE_QUOTE__()__SINGLE_QUOTE__))')) ~> extractyear"
			]
		}
	}
}